# {{.DisplayName}} project

This repository was scaffolded with `adr scaffold --lang {{.Language}}`.

## Development

```text
Build:  {{.BuildCommand}}
Test:   {{.TestCommand}}
Format: {{.FormatCommand}}
```

Read `AGENTS.md` and `.adr-scaffold.yaml` before making changes. Architecture
decisions belong in `docs/adr/` and can be initialized with `adr init`.
