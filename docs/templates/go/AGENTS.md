# AGENTS.md: Go project guidance

Use `{{.BuildCommand}}` to build, `{{.TestCommand}}` to test, and
`{{.FormatCommand}}` to format. Document exported APIs with {{.Documentation}}
and keep compiled binaries out of version control.

Read `.adr-scaffold.yaml` before changing Git habits or release automation.
