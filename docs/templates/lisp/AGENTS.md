# AGENTS.md: Common Lisp project guidance

Use `{{.BuildCommand}}` to build, `{{.TestCommand}}` to test, and
`{{.FormatCommand}}` to check formatting. Document APIs with {{.Documentation}}
and keep compiled Lisp artifacts out of version control.

Read `.adr-scaffold.yaml` before changing Git habits or release automation.
