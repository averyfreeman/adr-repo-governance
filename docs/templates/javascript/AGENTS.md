# AGENTS.md: JavaScript project guidance

Use `{{.BuildCommand}}` to build, `{{.TestCommand}}` to test, and
`{{.FormatCommand}}` to check formatting. Document public APIs with
{{.Documentation}} and keep package-manager output out of version control.

Read `.adr-scaffold.yaml` before changing Git habits or release automation.
