# Skill: HTML Code and Documentation

Use `{{.FormatCommand}}` as the baseline formatting command. Keep static page
structure accessible, use {{.Documentation}}, and keep generated site output
separate from source.
