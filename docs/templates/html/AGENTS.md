# AGENTS.md: HTML project guidance

Use `{{.FormatCommand}}` to check formatting. Build and test commands are not
defined for the static HTML profile. Document the page structure with
{{.Documentation}} and keep generated site output out of version control.

Read `.adr-scaffold.yaml` before changing Git habits or release automation.
